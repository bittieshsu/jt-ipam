# 相依套件漏洞的暫時豁免清單（CI 的 audit job 會讀）。
#
# 這個檔案存在的理由，是為了**不要**把 CI 的漏洞掃描改成 `|| true`。
# 掃了卻一律忽略，等於沒掃：真正該擋的那一次也會照樣通過。
#
# 每一行格式：
#   <漏洞 ID>  <到期日 YYYY-MM-DD>  <理由／影響面／緩解方式>
#
# 規矩：
# - **一定要有到期日**。沒有到期日的豁免會變成永久的，然後被忘記。
# - 理由要寫「為什麼這個環境不受影響」或「暫時無法升級的原因」，不是「之後再處理」。
# - 到期後要重新評估，不是自動延長。
#
# 目前沒有任何豁免。
