# 預編 guacd 用的原始碼。改這裡就是換版本 —— 換完五個目標都要重編、重驗（含實際連 RDP）。
#
# 為什麼暫時釘在 staging/1.6.1 的 commit 而不是正式版 1.6.0：
#   1.6.0 在 Ubuntu 26.04 上一連 RDP、畫第一個畫面就 segfault（在 libfreerdp3.so.3.31.0 裡；
#   同版 FreeRDP 在 24.04 正常）。staging/1.6.1（1.6.1 正在準備發版）五個目標實連都正常。
#   1.6.1 正式發版後改用 Apache 官方 tarball 並核對官方 sha512（TEST_CHECKLIST 5h 會提醒）。
GUACD_VERSION=1.6.1-pre.d9ec474
GUACD_SRC_URL=https://codeload.github.com/apache/guacamole-server/tar.gz/d9ec4742a454961eb40e1ee3aa6bd4d41e8a0b8d
GUACD_SRC_SHA256=4107cb39055f4484731a7c330cf60289f4a63856f3effaa45317b67a4009163a
# 同一份原始碼重新打包（改了編譯參數、修補、相依）時加一。3＝加上 SSH 主機金鑰類型修補（patches/0001）
GUACD_BUILD_REV=3
# 安裝位置：刻意放在 jt-ipam 的 git 工作目錄之外，git pull 升級不會碰到
GUACD_PREFIX=/opt/jt-ipam-guacd
