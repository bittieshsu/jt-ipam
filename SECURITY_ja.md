# セキュリティポリシー

セキュリティは jt-ipam にとって初日からの要件です。すべてのモジュールとすべての
プルリクエストを **OWASP Top 10:2025** のチェックリストに照らして確認し、
公開前には必ず OWASP ZAP のベースラインスキャンを新規の指摘ゼロで通すことを
リリースの条件としています。

English: [SECURITY.md](SECURITY.md) · 繁體中文: [SECURITY_zh-TW.md](SECURITY_zh-TW.md)

## サポート対象のバージョン

| バージョン | サポート |
|---|---|
| `0.6.x`（現行） | 不具合修正とセキュリティ修正 |
| `0.5.x` | セキュリティ修正のみ |
| `0.4.x` 以前 | サポート対象外 — アップグレードしてください |

`v0.6.8` 以降のリリースにはタグ（`vX.Y.Z`）が付いています。それ以前はタグ無しで
公開していました。もしそうしたバージョンを使っている場合は、そこに固定するのではなく
アップグレードしてください。

リリースの頻度が高いため、「サポート対象」とはその系列の最新パッチを指します。
修正は古いパッチへのバックポートではなく、新しいパッチリリースとして提供されます。

## 脆弱性の報告

**セキュリティに関する問題を公開の issue に書かないでください。**

次のいずれかで非公開にご報告ください。

- GitHub：[非公開のセキュリティアドバイザリ](https://github.com/jasoncheng7115/jt-ipam/security/advisories/new)を作成する
- メール：リポジトリのプロフィールに記載のメンテナ宛のアドレス

影響を受けるバージョン、再現手順、影響範囲を添えてください。数営業日以内の受領連絡を
目標とし、修正と公開の時期についてご相談しながら進めます。

## 主要な対策

- TLS は必須です（nginx のリバースプロキシ、または uvicorn の自己署名）。
- 機密情報（DNS の資格情報、SNMP、API トークン）はアプリケーション層で暗号化します。
  パスワードは argon2id、多要素認証は TOTP です。
- 外向きの連携先 URL はすべて SSRF の許可リストで制御します（メタデータやリンクローカルは遮断）。
- 監査イベントは SHA-256 で連鎖させます。

## 受容しているリスク（代替の統制つきで明記）

以下は、不釣り合いなコスト（たとえば UI フレームワークの全面的な入れ替え）無しには
取り除けない既知のスキャナ指摘です。監査上の透明性のためにここに残します。いずれも
周囲の統制によって実質的に無害化されています。

### CSP の `style-src 'unsafe-inline'`（ZAP ルール 10055 が *Medium* と判定）

- **取り除けない理由：** フロントエンドは Vue 3 + Naive UI です。Vue の `v-show`、
  動的な `:style` バインディング、Naive UI のフローティング要素の位置決めは、いずれも
  インラインの `style="…"` **属性**を出力します。CSP にはインラインの style *属性* を
  nonce やハッシュで許可する手段がありません（nonce とハッシュが効くのは `<style>`
  ブロックだけです）。しかも nonce を付けるとブラウザは `'unsafe-inline'` を*無視*するため、
  アプリ内のすべての `v-show` / `:style` が壊れます。これは CSP 自体の制約であり、
  主要な Vue / React のコンポーネントライブラリ（MUI、Angular Material など）に共通します。
- **実際のリスクが低い理由（代替の統制）：**
  - `script-src 'self'`（スクリプトには `unsafe-inline` を与えていません）→ 注入された
    CSS から JavaScript を実行することはできません。
  - `img-src 'self' data: blob:` と `connect-src 'self'` → 注入された CSS からデータを
    持ち出すことはできません（属性セレクタと外部画像を組み合わせる古典的な手口は
    遮断され、外部のオリジンには到達できません）。
  - 出力は Vue が自動的にエスケープするため、そもそも注入の入口がありません。
  - Naive UI の config provider で `inline-theme-disabled` を有効にし、テーマの
    スタイルをインライン属性から `<style>` ブロックへ移して、インラインの面積を
    最小化しています。
- **正味の影響：** 最悪でも見た目の CSS を書き換えられる程度で、コードの実行や
  データの窃取には至りません。`deploy/zap-baseline.conf` で管理しています。

## リリースの関門

リリースのたびに ZAP スキャンを実行し（HTTP と、公開のリバースプロキシ越しの両方）、
上記の記録済みベースライン（`deploy/zap-baseline.conf`）を超える指摘が**無いこと**、
つまり High / Medium / Low のいずれも新規ゼロであることを条件としています。

セキュリティ上の問題かどうか判断がつかない場合も、非公開でご報告ください。こちらで
切り分けます。
